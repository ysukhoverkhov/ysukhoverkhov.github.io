// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // User site served at the root of the domain — no `base` needed.
  site: 'https://yurysukhoverkhovditto.github.io',
});
